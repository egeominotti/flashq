# flashQ Production Environment Variables
# Copia questo file come .env.prod e configura i valori

# Caddy (email per SSL Let's Encrypt)
ACME_EMAIL=admin@flashq.dev

# Auth (comma-separated tokens per autenticazione)
AUTH_TOKENS=token1,token2

# Logging (debug, info, warn, error)
RUST_LOG=info

# Rate Limiting
RATE_LIMIT_REQUESTS=10000
RATE_LIMIT_WINDOW_SECS=60
